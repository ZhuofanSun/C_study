/**
*     printf("Hello world."); //定义这个函数的源文件是<stdio.h>
    // #include 就是引入的意思。引入了stdio.h才能使用printf
 * 以".h"结尾的文件是头文件
 * 头文件一般仅包含定义一类的简单信息，只要能让编译器认识就行了
 * # include"文件名称" // 引入的是自己写的，程序目录中相对路径中的头文件
 * # include<文件名称> // 引入的是系统库里的文件
*/