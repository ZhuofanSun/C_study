#include <stdio.h>

int main() {

    printf("Storage size for int : %zu bytes\n", sizeof(int));
    printf("Storage size for float: %zu bytes\n", sizeof(float));
    printf("Storage size for double: %zu bytes\n", sizeof(double));
    return 0;
}
